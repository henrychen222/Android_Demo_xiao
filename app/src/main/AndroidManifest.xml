<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weichen.myapplication"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="19" />
    <!-- 拨打电话权限 -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MusicActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AlipayActivity"></activity>
        <activity android:name=".CaculatorActivity"></activity>
        <activity android:name=".LayoutActivity"></activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".OtherActivity"></activity>
        <activity android:name=".QQActivity"></activity>
        <activity android:name=".RelativeActivity"></activity>
        <activity android:name=".TansuoActivity"></activity>
        <activity android:name=".WeActivity"></activity>
        <activity android:name=".WeiboActivity"></activity>
    </application>

</manifest>